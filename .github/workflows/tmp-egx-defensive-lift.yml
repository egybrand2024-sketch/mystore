name: Temporary EGX Defensive Lift Backtest

on:
  push:
    branches:
      - tmp-egx-defensive-lift-v32-dd2024-20260904
  workflow_dispatch:

permissions:
  contents: write

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout diagnostic branch
        uses: actions/checkout@v4
        with:
          ref: tmp-egx-defensive-lift-v32-dd2024-20260904
          fetch-depth: 1

      - name: Install research dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install numpy scikit-learn

      - name: Fetch public EGX historical dataset
        run: |
          rm -rf egxdata
          git clone --depth 1 https://github.com/mohamedredasafan1/dataroom.git egxdata
          echo "CSV files: $(find egxdata/Dataset/raw -name '*.csv' | wc -l)"

      - name: Run v3.2 2024 drawdown diagnostic
        run: python3 tmp/egx_backtest/analyze_v32_2024_drawdown.py

      - name: Commit diagnostic result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin tmp-egx-defensive-lift-v32-dd2024-20260904
          git add tmp/egx_backtest/results_v32_2024_drawdown_analysis.json
          if git diff --cached --quiet; then
            echo "No result changes"
          else
            git commit -m "Analyze v3.2 2024 drawdown"
            git push origin HEAD:tmp-egx-defensive-lift-v32-dd2024-20260904
          fi
